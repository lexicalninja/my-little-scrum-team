name: Test MLST Pi Extension

on:
  push:
    paths:
      - 'packages/mlst-pi/**'
      - 'skills/**'
      - 'templates/**'
  pull_request:
    paths:
      - 'packages/mlst-pi/**'
      - 'skills/**'
      - 'templates/**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: packages/mlst-pi
        run: npm install

      - name: Compile TypeScript
        working-directory: packages/mlst-pi
        run: npm run build

      - name: Run tests
        working-directory: packages/mlst-pi
        run: npm test
